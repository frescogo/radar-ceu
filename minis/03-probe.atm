require "radar"

spawn Probe(@1)

par {
    every _,on,dev in :radar.on {
        xprint(on, dev)
    }
} with {
    every _,err in :radar.err {
        ;;xprint(err)
    }
} with {
    every v in :radar.vel {
        ;;xprint(v)
    }
}
